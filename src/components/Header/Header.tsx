import SiteName from "./SiteName";
import Navigation from "./Navigation";

const Header = () => {
    return (
        <div>
            <SiteName />
            <Navigation />
        </div>
    )
};

export default Header;